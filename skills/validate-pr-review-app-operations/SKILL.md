---
name: validate-pr-review-app-operations
description: |
  Operate and observe a running validate-pr-review-app server. Covers the HTTP endpoints
  (GET /webhook, GET /ready health check), the JSON structured log format, log levels
  (INFO/WARN/ERROR) for alerting, and pointers to AWS Lambda / GitHub monitoring and webhook
  delivery validation docs.
  Use this skill for observability, health-check, logging, or monitoring questions about a
  deployed server — for example wiring up alerts by log level or checking the readiness endpoint.
---

Read [reference.md](reference.md) to operate a running server — the HTTP endpoints
(`/webhook`, `/ready`), the JSON log format and log levels for alerting, and links to AWS/GitHub
monitoring and webhook-validation documentation.
